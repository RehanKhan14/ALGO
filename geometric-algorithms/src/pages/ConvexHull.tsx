import { useEffect } from "react";

export const ConvexHull = () =>{
    useEffect(()=>{
        document.title="Geo Algorithmics | Convex Hull";
    },[])
    return (
        <>Convex Hull</>
    );
}