import { useEffect } from "react";

export const Intersection = () =>{
    useEffect(()=>{
        document.title="Geo Algorithmics | Intersection";
    },[])
    return (
        <>Line Intersection</>
    );
}